---
module_id: "deletion-recovery-retention"
title: "删除与数据保留"
group: "系统规则"
version: "1.0"
status: "review"
last_change: "PRD-0034"
summary: "负责人确认删除任务分支，文件区分删除与解除关联，本期不提供恢复。"
lifecycle_stage: "删除与数据保留"
pages: "任务详情, 文件详情, 删除影响确认"
objects: "task, task_deletion_impact, file, file_version, task_file_placement, file_deletion_impact, activity, audit_record"
operations: "preview_delete, delete_task, preview_delete_file, delete_file, remove_placement"
---

# 删除与数据保留

## 1. 目的

清楚告知删除范围及不可恢复的结果，避免误删任务和共享资料。

## 2. 范围和边界

- 本期任务和文件删除均**不可恢复**，不提供回收站、30 天保留承诺或当次撤销入口。
- 删除不是完成任务，也不是账号或整个团队的数据清除。

## 3. 详细功能设计

### 3.1 删除任务分支

#### 删除权限

- 只有待删除任务的当前**负责人**可发起。
- 删除包含该任务和所有后代，即使后代由其他成员负责。
- 父任务**负责人**不因此获得独立删除其他分支的权限。
- **参与者**不能直接删除子任务。
- 无**负责人**时，先由现有任务成员在人员编辑中设置负责人，再由负责人操作。
- 若已无任务成员，保留未分配记录，本期不增加管理员代删或责任恢复流程。

#### 删除流程

1. 查看根任务、**全部后代名称与数量**、关联文件及引用影响。
2. 确认“**删除后不可恢复**”，执行删除。
3. 权限或分支范围变化时，刷新影响并再次确认；取消则保持原状。

#### 执行结果

确认成功整体删除该分支，从列表、详情及个人范围移除，终止待加入任务的选择并通知受影响成员；任一写入失败则整体保持原状。网络结果未知时查询原操作，重试不重复删除。

### 3.2 文件与关联

- 解除当前任务中的文件关联只移除该处引用，不删除被其他任务使用的文件。
- 删除规范文件前显示有权查看的引用影响，跨任务共享文件只有操作者对受影响任务均有编辑权限时才可**整体删除**，否则仅允许解除本任务关联。

#### 删除后的保留内容

- 任务删除只解除其文件关联，不连带删除共享文件。
- 文件删除后不再供普通打开、下载或新引用，已有普通引用显示“文件已删除”，不提供恢复。
- 正式提交引用的**固定版本**与必要活动保留为历史记录，不作为文件恢复入口。
- 保留不扩大原有读取权限。

## 4. 功能验收标准

- 参与者直接删除任务被拒绝；负责人删除父任务时，确认界面包含所有后代，成功后整支均不可恢复。
- 预览后新增子任务时，旧确认不能直接执行；刷新范围并再次确认后才删除。
- 无负责人任务不出现可执行的删除动作，不能用团队管理员身份绕过负责人限制。
- 删除任务不删除其他任务正在使用的共享文件；解除关联不伪装为删除文件。
- 删除成功后无恢复／撤销入口，失败不移除任何半个分支，同次重试不重复通知。
