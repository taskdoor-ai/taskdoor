# TaskDoor PRD 编写与维护说明

本目录用于持续维护 TaskDoor 的目标产品需求。Markdown 是唯一内容源；HTML 和模块索引均由构建脚本生成，不在生成文件中直接修改内容。

## 内容结构

每个模块保留目的、范围和边界、详细功能设计、功能验收标准四个区块。

按功能说明核心用途、用户能做什么及操作结果。优先用一小段讲清，必要时用短列表。

- 模块标题使用稳定的功能名，直接回答“这里管理什么”，避免用首次进入、建立、生命周期结束、结果收口等过程性表述。相关模块归入产品概览、账号与团队、任务管理、协作与连接、系统规则五组；分组写在 `group` 元数据中。
- 用功能小标题组织正文；连续操作使用有序列表，并列规则使用短项目符号，关键动作、限制和结果适度加粗。复杂功能按“流程／规则与限制／异常处理”分组，简单功能不强行套齐小节；避免一段混写多个主题，也避免将整段文字全部加粗。
- 正文只写最终功能、用户操作、系统响应与必要规则；不得写入对话过程、修改指令、方案辩解或“取消旧设计”的叙述。修改原因归变更日志，实现差异归实现矩阵。正文、范围、图注及验收不得混入 Mock、本地演示、测试数据、尚未接入等开发状态说明；图片用于呈现界面与流程，来源及实现证据仅登记在来源映射、实现矩阵和截图清单中。
- 不逐项复述页面标题、按钮、字段排列、位置、尺寸或颜色；只有影响理解与操作的关键交互才写。
- 同一规则只在归属模块说明一次，其他模块引用；正文、图注和验收不重复铺陈。
- 目的只写用户价值，范围只写产品能力与业务边界，功能只写能力与关键规则，验收只补可验证条件。
- 技术校验细则按需引用参考文件，不将长篇细则搬到验收区；精简表达不取消已确认约束。
- 保留影响结果的期限、权限、确认及异常规则；区分当前实现与后续目标。

### 详细功能设计的组织顺序

默认按 **整体流程 → 分步处理 → 生成或展示的内容 → 规则与异常 → 界面案例** 编写：

1. **整体流程**：有分支、补问或回退的流程先给流程图；写清判断条件、分支结果和结束点。
2. **分步处理**：按操作顺序写“用户输入／系统处理／结果”，用有序列表或步骤表，避免按零散控件组织。
3. **内容结构**：用字段表说明输出包含什么、哪些可编辑、哪些可为空或由系统计算。
4. **规则与异常**：集中说明跨步骤的权限、数量、保存和提交规则；异常表写“触发情况 → 系统响应与下一步”。
5. **界面案例**：放在核心规则之后，用于对照典型分支，不重复整段功能说明。

简单功能可省略不适用的小节，不强行补图表。流程图负责看路径、表格负责查规则，正文只解释图表未表达的细节；同一规则只维护一处。示例轮数、人数和任务数不得变成通用限制。

## 图片规范

图片放在对应功能说明附近，存入 `assets/<module-id>/`。每张图必须包含清楚的替代文本、稳定图号和一句能独立说明图片用途的图注。图号格式为 `FIG-<模块缩写>-<三位序号>`，例如 `FIG-LIST-001`。

## 变更机制

语义需求变更必须在同一个 Pull Request 中同步更新：

1. 对应模块 Markdown；
2. 受影响的图片或示意图；
3. `CHANGELOG.md` 中的变更记录。

语义需求变更包括功能范围、交互、规则、状态、权限或验收标准变化。错别字、措辞润色和纯样式调整无需写入变更日志，由 Git 历史保留即可。

## 文件约定

- `index.md`：PRD 标题、说明、模块分组和顺序。
- `modules/`：按模块拆分的需求正文。
- `templates/module-template.md`：新模块起稿模板。
- `assets/`：与模块正文一起维护的图片。
- `CHANGELOG.md`：语义需求变更日志。
- `public/prd/`：自动生成的完整 HTML PRD 和 AI 模块索引。

运行 `node scripts/build-agentdoor-prd.mjs` 重新生成阅读页。生成前会校验模块元数据、四段结构、模块 ID 和本地图片；构建失败时先修复源 Markdown，不要直接修改 `public/prd/`。

## 产品名称维护

2026-09-21 起，产品名称统一为 **TaskDoor**，产品定义、PRD 阅读页、中英文界面及示例系统署名使用同一名称。本次仅调整品牌文字，不改变功能范围、流程、规则或验收标准，按非语义修正处理，不新增 PRD 变更 ID，不发布新版本。既有文件路径、Skill／MCP 标识、存储键及已发布归档保留，避免破坏链接与历史数据；这些技术标识不作为产品展示名称。


## 完整版维护基线

2026-09-21 按用户要求，将原 `.worktrees/full-product-prd/docs/prd` 的十五个模块整合至本目录，并保留第十六个多语言模块。当前唯一正文入口为 `index.md`，后续更新直接修改对应完整模块；`01-task-list.md` 和 `03-task-detail.md` 为早期局部稿，仅保留供旧日志链接追溯，不参与构建。原完整稿日志与来源保存在 `history/full-product-prd-baseline/`，其中变更编号属于独立历史，不能与当前日志混用。原中文运行截图已撤出阅读页，设计流程图继续作为目标示意。


## PRD-0034 本轮业务规则与术语

2026-09-23 按用户逐项评审更新本期需求。任务中的达成条件称“任务完成标准”，文档第四区块称“功能验收标准”，完整术语表见模块 15。用连续说明讲清功能和规则，用可判定场景验收；这两类标准不得混称。

本轮明确替代的历史规则：任务参与／负责人接受流程、独立任务 ACL 配置、负责人专属完成及强制核对、创建草稿恢复、手工创建入口、30 天回收站与文件撤销、任务交接阻断、固定上海日期及一分钟验证码有效期。正式模块和同步交互基线是这些规则的当前依据；历史归档及旧 API 提案仅供追溯，不能用其中被替代的条款覆盖本轮正文。未受影响的既有规则继续适用，不在本轮扩大产品范围或发布新版本。
