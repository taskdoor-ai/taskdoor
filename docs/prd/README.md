# TaskDoor PRD 编写与维护说明

本目录用于持续维护 TaskDoor 的目标产品需求。Markdown 是唯一内容源；HTML 和模块索引均由构建脚本生成，不在生成文件中直接修改内容。

## 内容结构

每个功能模块只固定四个一级内容区块：目的、范围和边界、详细功能设计、验收标准。详细功能设计不强制套用更多固定章节，应按页面和页面内模块连续说明以下信息：

- 模块展示什么；
- 用户可以执行哪些操作；
- 每项操作的步骤和结果；
- 只有确有必要时，补充排序、分组、分页、状态、异常或权限规则。

同一功能的展示、操作和结果应写在一起，避免让读者跳转到文档其他位置寻找对应步骤。

## 图片规范

图片放在对应功能说明附近，存入 `assets/<module-id>/`。每张图必须包含清楚的替代文本、稳定图号和一句能独立说明图片用途的图注。图号格式为 `FIG-<模块缩写>-<三位序号>`，例如 `FIG-LIST-001`。

## 变更机制

语义需求变更必须在同一个 Pull Request 中同步更新：

1. 对应模块 Markdown；
2. 受影响的图片或示意图；
3. `CHANGELOG.md` 中的变更记录。

语义需求变更包括功能范围、交互、规则、状态、权限或验收标准变化。错别字、措辞润色和纯样式调整无需写入变更日志，由 Git 历史保留即可。

## 文件约定

- `index.md`：PRD 标题、说明和模块顺序。
- `modules/`：按模块拆分的需求正文。
- `templates/module-template.md`：新模块起稿模板。
- `assets/`：与模块正文一起维护的图片。
- `CHANGELOG.md`：语义需求变更日志。
- `public/prd/`：自动生成的完整 HTML PRD 和 AI 模块索引。

运行 `node scripts/build-agentdoor-prd.mjs` 重新生成阅读页。生成前会校验模块元数据、四段结构、模块 ID 和本地图片；构建失败时先修复源 Markdown，不要直接修改 `public/prd/`。

## 产品名称维护

2026-09-21 起，产品名称统一为 **TaskDoor**，产品定义、PRD 阅读页、中英文界面及示例系统署名使用同一名称。本次仅调整品牌文字，不改变功能范围、流程、规则或验收标准，按非语义修正处理，不新增 PRD 变更 ID，不发布新版本。既有文件路径、Skill／MCP 标识、存储键及已发布归档保留，避免破坏链接与历史数据；这些技术标识不作为产品展示名称。


## 完整版维护基线

2026-09-21 按用户要求，将原 `.worktrees/full-product-prd/docs/prd` 的十五个模块整合至本目录，并保留第十六个多语言模块。当前唯一正文入口为 `index.md`，后续更新直接修改对应完整模块；`01-task-list.md` 和 `03-task-detail.md` 为早期局部稿，仅保留供旧日志链接追溯，不参与构建。原完整稿日志与来源保存在 `history/full-product-prd-baseline/`，其中变更编号属于独立历史，不能与当前日志混用。原中文运行截图已撤出阅读页，设计流程图继续作为目标示意。
