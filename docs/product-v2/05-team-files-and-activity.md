# Task 文件与任务外协作动态

> 状态说明：当前产品表面只保留 Task 内文件页，不再提供独立团队文件模块；File / FileVersion、Task 内引用与 ACL，以及人际 Handoff 的安全语义不变。task-scope File 同 ID 发布、团队动态导航、Handoff Manifest 与默认范围仍是建议默认，见决策台账。

## 一、Task 文件的定位

D-142（2026-08-31）明确 Task 内讨论与活动分离。其“文件正文引用发送后进入讨论页签”的呈现方式由 `FILE-DISCUSSION-2026-09-13-v1` 局部替代：选中内容写评论，点击“确认”后留在文件并打开右侧讨论栏；完整评论、回复与结论在文件内保存，不自动发到任务动态或可见活动。底层审计保留，真实文件提交／更新仍可在活动中展示操作人、时间和链接。无权或缺失的文件不可点击，不复制内容或扩大权限。

2026-09-13 的“动态”需求指当前 Task 的成员讨论，不恢复下文仍待确认的任务外广播。本次设计默认：动态、评论、回复及文件讨论的附件在正式发布成功时自动建立任务文件关系；已有文件保持同一 ID／原位置，新文件默认放 Task 根目录，手动上传／引用继续可用。消息引用固定 FileVersion；删除消息不删除任务文件，暂存上传失败或未提交不进入正式文件列表。完整目标操作、权限、通知和幂等规则见 [文字版 PRD 第 6.5、6.8、6.9 节](./PRD-AgentDoor-协作任务全流程.md#65-讨论)。

2026-09-14 回复交互修订：动态移除单独的“引用”操作和重复摘录，保留可定位的“回复 姓名”；文件讨论的原文锚点及引用保留。附件统一使用“附件”下拉，其中“上传本地文件”在发布后自动入任务文件列表，“从文件列表选择”通过多级文件夹子菜单选择已有文件、复用原文件；顶部搜索按文件名和所属路径跨层级查找，结果展示完整文件夹路径，清空回到层级浏览；点选即收起并回到正文，已添加文件显示勾选。

**当前 Demo（2026-09-15）：** 已接入 Lexical 0.50.0（MIT）的输入、文本及 PDF 提取正文阅读与 MarkNode 划词高亮；阅读／编辑均可选区，未保存修改先“保存文件并评论”，锚点保留页码／版本／范围；顶部“查看讨论 N”连接右侧讨论，划词“评论”连接右侧输入；顶部不再展示下载、添加评论及常驻说明，标题与正文左侧对齐；“确认”只保存文件右栏评论。动态／回复附件自动入文件列表、手动上传和已有文件选择、稳定 ID 的 @、具体回复／文件内引用、作者编辑／删除、解决／重开已实现。消息／附件关系按团队与 Task 写入同一份 localStorage 快照，上传原文件二进制保存在 IndexedDB，刷新可恢复；共用 File ID，引用保留 vN，删除消息不删文件。

**实现边界：** 无服务端 ACL、多人同步和外部通知，本地通知记录未接现有全局通知 UI；旧版全文快照预览尚未实现，保留引用版本／原文并明确展示当前最新正文。真实 DOCX／XLSX 等上传未解析，仅支持下载原文件；未提取正文的真实 PDF 使用浏览器预览，仅支持整文件讨论。下文发布协议、安全规则及验收项仍是目标，不据此宣称 Demo 已生产化。

File 是在团队命名空间中按策略可发现、可组织、可授权、可版本化的信息层。当前产品不为它建立独立一级模块，而从 Task 详情“文件”页进入；这不把 File 压成 Task 字段，也不要求 TaskDoor 替代企业已有网盘。

Task 文件页解决：

- 当前 Task 正在使用哪些文件与引用。
- 文件的当前有效版本、来源和任务内组织位置是什么。
- 当前用户和其 Agent 分别可以访问到什么程度。
- 文件发生更新时，当前 Task 是否可能受到影响。

## 二、团队文件与任务文件的关系（发布协议待确认）

```text
团队文件 / 零售业务 / POS / 优惠券规则 v3.2
             ↑ 同一 File ID
任务 A / 文件 / 需求与规则 / 优惠券规则（跟随最新）
任务 B / 文件 / 证据 / 优惠券规则（固定 v3.1）
```

- File / FileVersion 是唯一内容真相；File 当前可以处于 task scope 或 team scope。
- 团队文件夹是已发布 File 的规范组织位置；任务文件夹是面向当前目标的投影视图。
- 任务可以固定版本，也可以跟随最新版本。
- 任务自产文件先以 `scope=task` 的同一个规范 File 存在。发布到团队时默认保持 File ID，原子提升为 `scope=team` 并选择团队文件夹；该动作需要明确确认新的 audience / 权限范围，可能显式扩大团队可发现或可访问范围。
- 只有用户明确选择“衍生副本”才创建新 File ID，并记录 `derivedFromFileId`。衍生后两份文件不承诺自动同步。

## 三、Task 文件页

### 左侧

- 展示当前 Task 内的文件夹与文件层级，不承担 Task 分组或状态语义。
- 不提供团队文件夹侧栏、最近访问、与我共享等独立模块入口。

### 主区

- 直接展示当前 Task 中按 ACL 有权可见的文件与引用，不按协作人员切换，也不因选择某个人而裁剪文件树。
- 名称、类型、维护人、当前版本、更新时间与任务内位置读取同一 File / FileVersion / TaskFilePlacement 真相。
- 点击文件进入同一 File 的预览或详情，不创建内容副本。

### 文件详情

- 预览和结构化摘要。
- 版本历史与当前有效版本。
- 维护责任与可见范围。详情不设置统一“编辑”入口：点击维护人身份单元直接使用共享人员选择器，点击可见范围标签直接打开范围 Dialog；指定人员至少 1 人。两项修改都只更新当前应用会话，不执行真实维护责任转移、ACL、授权、撤权、持久化或审计。
- 详情不显示独立“返回文件列表”按钮，Task 文件页返回继续由保留上下文的面包屑承担。
- 当前 Task 如何引用文件、固定或跟随哪个版本。
- 与当前 Task + File 相关的标注和讨论，右侧集中查看、回复、@ 与定位原文；跨 Task 不自动合并。讨论绑定源版本，原文变化时明确提示，评论“确认”不等于业务决定或正文更新。
- 添加文件或引用前显示将共享的范围。

## 四、文件夹规则

- 文件夹只负责组织文件，不承担任务状态或责任。
- 禁止移动到自身或后代。
- 删除非空文件夹前必须选择迁移、归档或逐项处理。
- 同一 File 在当前 scope 内只有一个规范位置；跨任务和跨视图使用 Placement / 快捷引用，不复制真相。
- 文件夹权限可以继承，但文件允许收紧，不允许无提示放宽。

## 五、人与 Agent 共用的 Handoff Manifest

Handoff 时不克隆完整文件树，也不复制整段聊天。人类接收者和 Agent 都先得到经过权限过滤的最小 File List；Agent 再按任务需要请求正文。[11-human-handoff.md](./11-human-handoff.md) 的 `HandoffContextItem` 是唯一规范 Schema，下面只是接收者可见的 Manifest UI 投影：

```ts
type HandoffManifestProjection = {
  fileId: string;
  title: string;
  purpose: string;
  versionMode: "pin-version" | "follow-latest";
  resolvedVersionId: string;
  contentDigest: string;
  permissionBasisDigest: string;
  receiverAccess: "metadata" | "summary" | "read" | "edit";
  freshness: string;
  retrievalHint?: string;
};
```

- 关键证据、批准依据和交付物默认固定 FileVersion；持续编辑文件可以跟随最新，但激活时记录实际解析版本。
- 发出者可先看“接收者视角预览”，确认对方真正能看到哪些标题、摘要和正文。
- `receiverAccess=hidden` 只存在于发送者预检结果，不进入接收者 Manifest 投影；接收者端不得返回原 `fileId`、标题、来源或数量，只能提示“必要上下文尚未就绪”，避免元数据泄露。
- Handoff 等待期间若关键版本发生实质变化，显示 diff 并使依赖该版本的旧 Consent 失效。
- 私人对话不自动进入 Manifest；需要共享时先形成有来源、受权限控制的摘要对象。

Agent 按任务需要请求正文。系统以 Agent Principal 为实际 actor，并记录其代表的成员，然后再次检查：

- 当前用户权限。
- Agent Connector 权限。
- 任务作用域。
- 文件版本与敏感级别。
- 请求用途。

“用户能读”不自动等于“该 Agent 能读”；同样，“发出者能读”不等于“接收者能读”。实际权限是主体授权、委托、对象/版本策略与 Task scope 的交集；撤销委托或授权后下一次请求立即失效。Handoff 和 TaskFilePlacement 本身不授予权限，发送前和激活前都必须以接收者身份重新检查。

## 六、团队动态模块的定位

团队动态不是按时间排序的所有系统日志。它是任务之外的协作发现空间，帮助团队暴露需求、经验、风险和机会。

建议一级类型：

- 求助：我缺少信息、经验、权限或执行支持。
- 提供帮助：我在某个范围有经验或时间窗口。
- 发现：某任务中出现可能影响其他工作的结论。
- 风险：多个任务存在共同阻塞或文件冲突。
- 机会：可以跨团队共同探索的新方向。

系统变更日志不进入团队动态主流，只在审计或对象活动中查询。

## 七、求助广播生命周期

```text
draft → published → responding → matched → converted/linked → closed
                    ↘ no-response → refine audience / clarify need
```

### 发布前 AI 辅助

- 把模糊问题整理成具体缺口。
- 提取已有信息和缺失信息。
- 检查广播内容是否泄露任务或文件权限。
- 建议合适可见范围。
- 提醒是否已有相似问题、文件或可直接邀请的人。

### 响应方式

- 我可以提供帮助。
- 我知道谁更合适。
- 我有一份相关文件。
- 需要补充信息。
- 关注结果，但不承担责任。

响应不自动成为任务参与者，也不自动获得文件权限。

### 收敛

- 选择响应者后，用户按语义进入私密路径：新工作发协作邀请，接受前不添加参与者、不产生 Responsibility；已有上下文或责任接续则直接发出 Handoff Offer，不重复一张普通邀请。
- 需要持续推进时，动态可以创建新任务或关联已有任务。
- 当前产品以 Task 为唯一行动对象，不再从动态创建 Todo。
- AI 自动总结有效回答和未决问题。
- 关闭时记录帮助来源并给予可见贡献，但不做排行榜。

## 八、信息噪音与心理安全

企业社交网络能够通过可见沟通提高“谁知道什么”的元知识，但也可能制造噪音、打断和透明压力。

设计约束：

- 广播必须选择作用域，默认不面向全公司。
- 用户可以关注主题而不是关注全部动态。
- AI 合并重复问题，摘要长讨论，提醒仅推给高相关成员。
- 允许私密回应和私密拒绝。
- 不公开谁看过但没有回应，避免制造道德压力。
- 不把响应次数做成绩效榜。
- 敏感问题先推荐定向邀请，不进入公开动态。

研究依据：可见沟通可提高员工对“谁知道什么、谁认识谁”的准确度，并降低后续知识转移摩擦；与此同时，企业社交工具也存在数字中断与透明偏好差异。[Leonardi 2015](https://aisel.aisnet.org/misq/vol39/iss4/3/)、[Leonardi 与 Meyer 2015](https://doi.org/10.1177/0002764214540509)、[企业社交媒体设计研究](https://doi.org/10.25300/misq%2F2023%2F16499)。

## 九、权限检查

发布动态时分别检查：

- 动态文字本身的可见范围。
- 引用任务是否可发现。
- 引用文件元数据是否可见。
- 文件正文是否可读。
- 回复者加入任务后获得什么权限。
- AI 是否可以读取和汇总回复。

当权限不足时，系统应允许发布抽象后的问题，例如隐藏客户名和原文件标题，而不是直接扩大权限。

## 十、Demo 验收目标（当前覆盖范围见第一节）

- Task 详情“文件”页可以浏览当前 Task 的文件层级并打开版本历史，且不提供按人切换。
- 一级导航与全局侧栏不出现独立团队文件模块。
- 三个权限测试账号分别看到“完全隐藏”“只见安全元数据”“可读正文”，不能仅靠前端按钮隐藏模拟。
- 同一文件在两个任务中分别以跟随最新版和固定版本出现。
- 文件更新后，AI 提醒受影响任务，但不自动替换固定版本。
- 任务自产文件发布到团队后保持原 File ID；显式衍生时才产生新 ID 和血缘。
- 当前 React 原型不从任务意图输入壳发布求助广播；未来若恢复创建或广播入口，必须重新确认范围与正式写入边界。
- 响应者提供文件或推荐人，但不会自动加入任务。
- 用户选择响应者、确认权限并发出邀请。
- 已有工作交接时，接收者能看到最小 Manifest、复述范围；关键版本变化会阻止旧协议激活。
- 广播最终关联任务并由 AI 形成可追溯摘要。
