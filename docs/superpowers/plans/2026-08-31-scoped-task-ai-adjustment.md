# 模块内 AI 调整实施计划

> 用户已确认本方案。使用 writing-plans 制定计划，在当前共享工作区实施；不提交、不迁移工作树、不覆盖其他任务的改动。

## 结果与边界

移除创建页底部「继续调整」，在任务信息头部、子任务模块和单个子任务菜单放置共享「AI 调整」入口。任务信息和子任务范围固定，打开即带上下文。输入要求 → 预览具体前后值 → 明确应用。创建页应用到草稿，详情页保存已有任务并记活动；不重新创建整棵任务树，不修改状态或自动验收。初始 AI 创建流程、五个 Mock 场景、草稿缓存及旧对话归档不变。

当前继续使用确定性的本地 Mock：支持单字段改名／目标／完成标准／执行建议／负责人／日期，模块内指定子任务调整、显式增加子任务及调整真实前置依赖。没有模型推理服务；自由拆分、删除任务与复杂批量重排未支持时明确提示，保留原文，不制造结果。负责人修改保留待接受语义。只使用真实字段，旧详情的示例标准不自动转为正式数据。

## 视觉与复用

沿用当前 TaskDoor 蓝白视觉和已适配 21st 的人员／日期／Accordion 组件；共享 Base UI Dialog 负责焦点、Esc 和遮罩，Button 的 ai 变体负责入口。使用现行 Geist／中文系统字体，标题 medium、正文 regular、数字使用等宽特性，不引入新字体或依赖。

视觉签名为「范围标识 + 字段差异纸片」：紧凑蓝色作用范围标识，输入与预览在一个弹窗中连续展开；变更前灰色、变更后蓝色并标注文字，不只靠颜色。桌面复用共享 544px 宽度、窄屏可滚动；固定可达的取消／应用区域。使用现有 `--ad-*` 的 surface、text、route-blue、border、radius 和 shadow Token。任务页的主体布局和 D-148 左侧列表不变。

## 工作分解

### 1. 明确契约并先测纯逻辑

- 新建 `src/lib/taskAiAdjustmentTypes.ts`：稳定任务 ID、task/subtasks/subtask 作用范围、可编辑字段、候选 patch、前后差异。
- 新建 `src/lib/taskAiAdjustment.ts` 与 `server/taskAiAdjustment.test.ts`：先验证未实现失败，再实现有限语法、真实成员精确匹配、依赖 DAG、范围防越界、无变化、过期指纹、日期和继承目标限制。
- 单次指令不得部分执行复合要求；子任务模块支持指定项和显式新增，绝不猜测关联对象。调整只产出候选。

### 2. 共享弹窗与创建草稿

- 新建 `src/components/TaskAiAdjustmentDialog.tsx`、`src/styles/task-ai-adjustment.css`。
- 新建 `src/lib/taskAiAdjustmentAdapters.ts`、对应测试：CreationForm → 真实上下文 → 候选字段 patch；保留稳定 ID、人工字段、归属信息。过期方案拒绝应用。
- 修改 `TaskCreationPage.tsx`、`TaskCreationPlanEditor.tsx`：替换底部调整区为三个模块入口，共用弹窗；创建与修订明确分离，最终创建仍需原校验。
- 更新 `server/taskCreationPage.test.ts` 并添加共享入口测试，先红后绿。

### 3. 已创建任务的保存

- 修改 `src/App.tsx`、`TaskDetail.tsx`、`TaskSubtaskList.tsx`：传递真实任务上下文和保存回调，入口不打断行导航。任务详情本地字段随保存结果更新。
- 在适配器中校验当前快照、稳定 ID 和依赖，生成完整下一状态；无变化不写。只更新选择的字段，不覆盖既有状态、历史、标签、文件。
- 增加真实 `task-definition-change` 活动类型（类型、解析、显示），记录实际变化。负责人仍是提议，不替换正式 Owner。
- 节点、活动、提议和历史快照按一组本地写入处理；写入失败回滚本次键值且不更新 UI，不报告已保存。保留输入、预览、错误和重试入口。

### 4. 审阅与验证

- 独立代理先审核范围契约，再审核代码质量；有问题修复并复审。
- 运行 `node --import tsx --test` 相关测试、`npm run build`、`git diff --check`。
- 浏览器隔离端口验证：创建页三个入口、应用前后、取消／过期／不支持、详情保存并刷新、真实前置依赖循环拒绝、待接受负责人、活动分离、窄屏滚动及键盘焦点。
- 同步 D-149（写入前重新检查编号）、设计系统和设计包状态；本地 Skill 继续按决策台账确认现行范围，不扩写完整规划服务协议，明确这是模块调整而非恢复初始创建弹窗。

## 执行记录

- 2026-08-31：用户确认方案，读取现行 D-147/D-148 与设计／规划 Skill，开始实现。当前工作区已有其他改动，按文件边界协作并保留。
- 已实现：共享范围弹窗、创建三个入口、详情三个入口、前后差异、显式新增子任务、真实活动与待接受提议。旧底部调整区已移除；初始五场景 AI 创建与旧对话归档保持。
- 回归中修正：legacy 开始日期保留；新增 ID 与所有节点防碰撞；正式负责人／提议分开显示且撤回不复活；独立打开深层子任务时校验祖先、后代、截止日与循环；清空日期再次请求不写重复活动。保存使用可恢复的本地 journal，不宣称跨端事务或生产审计。
- 2026-08-31 验证：336 项相关测试通过（0 失败、0 跳过），`npm run build` 通过，`git diff --check` 通过。保留 Vite 原有 native config / 大包警告，未为本次安装新依赖。
- 隔离浏览器 5390 验证：创建草稿改名／单项改名与依赖名称同步／显式新增／取消不应用／不支持保留；详情完成标准保存、负责人待接受与撤回、真实活动不进讨论、新增一项后刷新仍为同一主任务；循环依赖拒绝。390×844 窄屏无横向溢出，固定范围和底部操作可见，按钮至少 44px，Esc 关闭后焦点返回入口。验证数据只在隔离本地端口，不写生产。
- 最终浏览器复核：独立进入新增子任务，超过主任务截止日与依赖主任务均被拒绝；控制台无错误。隔离验证服务已停止，保留用户原有 5173 服务；展示该服务中的模块弹窗但没有提交修改。
- 审核说明：前期审核指出的 legacy 日期和真实父级上下文问题均已补测试修复。收尾独立代理未返回最终结论，不将其计为独立审核通过；本次交付验证依据为上述主线程检查、自动化测试和浏览器实测。
