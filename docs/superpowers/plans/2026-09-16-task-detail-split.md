# 任务详情固定左右布局实施计划

**Goal:** 将已确认的预览更新到实际项目。任务信息与协作独立滚动，目标和完成标准优先展示，右侧始终展开。

**Architecture:** 保留 TaskDetail 的数据与编辑回调，重组为紧凑标题栏、左侧任务文档、右侧协作面板。复用现有进度计算、文件浏览和讨论组件；仅增加展示选项。移动端切换任务/协作，通知按目标定位。

**Tech Stack:** React、TypeScript、现有 UI 组件及 CSS。

本次直接在用户指定的实际项目工作区实施，保留已有未提交修改；依据 AGENTS.md 只做必要验证，不运行全量测试或双重审查。

- [x] 重排任务详情、次要属性对话框与移动端/通知定位；保留返回列表与父任务导航。
- [x] 调整进度展示、子任务信息和讨论输入框位置，复用现有真实数据。
- [x] 类型检查、相关回归测试、浏览器桌面/窄屏交互检查。

验收：首屏可见目标/标准及完整讨论；左侧长内容不挤压右侧；无右侧收起操作；草稿跨标签保留；文件和子任务入口有效；未知进度仍显示未知。


验证记录：

- `npx tsc -p tsconfig.app.json --noEmit` 通过。
- 任务活动路由、子任务渲染/依赖、进度模型相关 56 项测试通过；新增紧凑进度展示与未知数据 2 项测试通过。
- 浏览器验证桌面 1440 × 778、手机 390 × 844；目标/标准、属性对话框、文件目录与预览、活动、草稿跨标签保留、空状态、返回列表、父任务定位子任务均可用。
- 展开图表后左侧内容高度从 791 增至 1316，右侧位置与高度保持不变；页面没有外层纵向溢出。
- 已与改动前文件的隔离副本比较：活动渲染及旧工作量断言共 11 项既有测试失败，改动前同样失败。本次没有扩大修复范围。


2026-09-17 用户纠正外层布局：任务列表应常驻左侧，进入桌面工作区默认选中可见列表第一项，详情只占右侧面板。已恢复首项选择与列表/详情外层两栏，详情内部保留已确认分组。手机端仍通过返回按钮回到列表。

修正验证：类型检查通过；桌面浏览器 1280px 下左侧列表为 350px，右侧详情为 930px；初次进入和刷新均自动选中列表第一项；切换第二项后列表仍可见，右侧标题与选中项一致。

2026-09-17 进度摘要更新：用户确认将独立预览中的阶段名称、明亮蓝色四段刻度和紫色「AI 预测」接入实际项目，仅调整进度展示。主任务不再显示百分比、L1–L4 或重复的说明标题；子任务使用阶段文字与来源。原燃起图组件、图表样式、底层数值及日期计算保持不变。

展示映射兼容现有完成量：有依据的 0 为「未形成成果」，大于 0 且不足一半为「部分完成」，一半至不足九成为「大部分完成」，九成及以上为「接近完成」。这是粗粒度展示，不回写记录。只有用户确认状态显示「已确认完成」；未知显示「暂无评估」，取消记录保留「取消时进度」来源。

必要验证：类型检查通过；进度档位、预测与确认区分、未知状态、图表输出一致性、子任务与原进度模型共 24 项相关测试通过。浏览器已核对主任务/子任务摘要、紫色来源标注及展开燃起图。未运行全量测试或双重审查。

2026-09-17 燃起图显示比例修正：此前仅比对 SVG 内容，未发现左右布局把原窄栏图表拉宽。1440px 桌面实测，320 单位的 SVG 被展示为 494.29px，刻度与节点放大约 1.54 倍。现将详情内燃起图容器限制为不超过 320px，保留随窄屏收缩，并恢复原图表上间距。图表数据、日期布局、阶梯线及悬浮信息不变。

必要验证：在真实浏览器检查「确认下周行动项与负责人」及「完成秋季抗老精华首发闭环」。修正后 SVG 宽度为 320px，11px 刻度按原尺寸显示；完成任务的延期 16 天、创建/截止/完成日期和节点明细可见，进行中任务的历史与预测阶梯线、总工作量延续线、预测日期及 Escape 关闭提示均正常。阶段进度和紫色 AI 来源仍保留。本次仅修改容器 CSS，采用浏览器验证，未重复运行全量测试。

2026-09-17 补齐预览迁移：用户指出实际头部缺少已确认的渐变和字段对齐，子任务缺少分段进度与计划/预计时间差。头部现按任务颜色渐变至白色，属性行与图标左边对齐；负责人通过头像组件的尺寸参数使用 28px，修复原头像实际 36px、容器仅 28px 导致的姓名重叠。标题输入改为块布局，消除基线多余空间。

子任务保留四段蓝色进度和紫色 AI 来源，复用原时间轴展示开始、观测、计划完成和预测/确认完成日期，以及提前、按期或延期。明确替代此前「子任务只显示档位文字」的简化；缺失预测不推断完成日。当前计划日期参与对比，预测日期仍来自独立记录。

验证：类型检查、23 项进度与子任务相关测试通过。浏览器 1440px 与 441px 检查头部对齐、渐变、子任务进度和时间轴，无页面横向溢出；负责人头像与姓名间距 8px。更多属性、截止日期入口及 Escape 关闭正常。燃起图宽度仍为 320px，预测节点与细节保留。未运行全量测试或双重审查。
