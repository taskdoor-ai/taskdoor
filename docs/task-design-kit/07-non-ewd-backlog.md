# TaskDoor 非 EWD 待办清单

**这是按用户要求落地的本地待办清单，共 15 项，全部尚未勾选。**沿用原14项，按用户最新要求新增G16任务成本分析，并把个人优先级、成本与EWD联合设计。本次授权是分析与本地待办整理；不等于批准生产实施、真实模型接入、新入口或持续运行。本文件未创建外部任务、未发送消息、未正式指派成员，也没有约定交付或会议日期。

本清单沿用 [05 当前差距分析](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/05-current-gap-analysis.md) 的 G 编号。**G07的EWD、G10/G11的个人优先级与G16的任务成本统一在 [06 联合方案](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/06-ewd-design.md)设计**；这里保留执行跟踪，不各自另立工时、成本或排序口径。EWD专属估算、验收计量、叶子聚合及Burn up实现仍只列06的EW待办。

**每项为什么成为待办：原始要求／能力方向 → 当前可核实的差距 → 最小补齐工作。**下面为每项补充一手来源、跟进理由和推导边界。会议明确待办、会议方向、老板文件中的能力主张/未来机会、助手的工程拆解是不同性质，不能统一写成“会议已要求开发”。源码只能证明现状差距，不能自己创造业务需求；05/06和验收文档是整理产物，不能代替原始来源。

**来源索引：会议与用户补充。**会议来源是用户在本任务首条请求中贴入的纪要，会议日期未提供。下表保留对应位置与短原文摘录，不是重新编造会议记录；后续版本不应覆盖这些摘录。

| 来源编号 | 原文位置与摘录 | 来源性质 |
| --- | --- | --- |
| M-API | 纪要“待办”第1条：“梳理并确认任务创建过程中需要对接的真实API列表。”原列卜佳菲 | 会议明确待办；只直接覆盖任务创建API，其他接口是配套扩展 |
| M-SKILL | 小结4：“通过Skill验证大模型对任务拆解、目的推理及负责人推荐的准确性”；待办：“整理并输出单任务与复杂任务的详细规划Skill解析逻辑与JSON返回结构示例。”后者原列任向晖 | 会议核心方向＋明确产出 |
| M-CREATE | 小结1：“自然语言的任务创建、编辑与查询”；“AI可自动判断并配置子任务的前置依赖项”；“基于当前用户权限识别类似或重复任务并进行提示” | 会议功能方向；不等于批准任何特定后端实现 |
| M-DETAIL | 小结2及待办：紧凑头部卡片；AI洞察需“独立为Tab页或与子任务并列”；“严格区分人的讨论（Discussion）与系统动态（Activity Log）”；前置依赖的“显性呈现价值极低” | 会议设计方向；头部/洞察原待办列卜佳菲 |
| M-EWD | 小结2：“抛弃简单的任务个数完成度计算”，引入Burn up与EWD；待办：“设计基于工作量的任务完成度评估指标。”工时/图表事项原列卜佳菲 | 会议明确需求与待办；未提供叶子公式、验收算法或字段结构 |
| M-HOME | 小结3：“MVP版本直接以任务列表为主”；移除注意力地图和通知聚合，导航精简 | 会议减法方向；已有实现后应核验，不再完整重做 |
| M-PERSON | 小结3：“重要紧急程度分析仅针对个人负责的所有任务在工作空间层级进行” | 会议明确分析范围；未给出排序公式或Dashboard布局 |
| M-REPORT | 小结4：“定义统一的客户端回传规范”，校验成员本地“MCP连接状态及心跳时间戳” | 会议明确能力方向 |
| M-CLOUD | 小结4：“最终目标是承载企业级云端Agent的持续运行与治理” | 会议长期方向，不是本轮直接实施待办 |
| M-REVIEW | 待办最后一条：“下周某日再次召开会议，集中Review Skill的逻辑设计与调优方案。”原列任向晖、卜佳菲 | 会议明确后续事项；具体日期未定，不能推算为已预约 |
| U-ANALYSIS | 用户首条消息中的补充：“第一个是分析目前系统的差异，第二个是分析我们还缺哪些东西”，并提到个人应该推进什么、工时、完成度和重要紧急分析 | 用户分析需求；嵌在纪要内的补充不自动称为会议决议 |
| U-JOINT | 用户后续明确：“个人维度的，优先级 紧急层度”“任务维度的：任务下成本分析”“这两个任务和 EWD 一起分析” | 本轮直接分析要求；新增任务成本来源于此，不伪装为原会议或老板文件已定模块 |
| U-PM | 用户后续要求：“深度熟悉项目管理的方法论与核心，pmp 的任务管理核心” | 要求以项目管理方法支撑方案；06核对PMI公开资料，具体产品规则仍是提案 |

**来源索引：老板文件。**原件为《TaskDoor AI 协作能力参考.html》，页脚2026-08-30。下列链接直接指向原文件；“已经实现”只表示原文件这样声明，当前仓库是否实现还需另外验证。

| 来源编号 | 文件中的具体条目 | 来源性质 |
| --- | --- | --- |
| F-PLAN | [第2节：任务输入、目标与计划、成员推荐、动态规划](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:532>)，包括预计工作量、独立验收、职责推荐与已有上下文重规划 | 原文件的当前能力主张，不是本项目通过证据 |
| F-INTEL | [第2节：任务智能、多语言](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:536>)：Current Status、下一步、AI标签；多语言条目在同表末行 | 当前能力主张；具体纳入范围见文末覆盖说明 |
| F-SESSION | [第2节：个人Agent、Agent请求](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:537>)：权限受限会话，由Agent所有人决定是否启动 | 当前能力主张与授权边界 |
| F-WRITE | [第2节：成果沉淀、协作安全](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:539>)，以及[第4节产品边界](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:582>)：文件版本、差异/归因、权限/版本/幂等，人对结果负责、不静默改边界 | 当前能力主张＋产品边界；未指定React/MCP统一方式 |
| F-QUALITY | [第3节Priority 1](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:553>)：风险、成功标准检查、讨论/活动摘要、通知摘要；[第5节](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:593>)推荐“风险识别＋成功标准检查＋每日协作摘要” | 未来机会与推荐顺序，不是已批准实施 |
| F-SCHEDULE | [第3节Priority 2](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:562>)：跨任务依赖、容量推荐、Workspace优先级、计划偏差复盘 | 未来机会；个人范围继续受M-PERSON与现行决定约束 |
| F-MULTI | [第3节Priority 3](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:571>)：更多客户端、专业Agent分工、授权后的定时/事件执行、组织经验复用 | 未来方向，不自动授权持续运行 |
| F-VERIFY | [第6节当前状态说明](</Users/yxzuji/Downloads/AgentDoor AI 协作能力参考.html:603>)：真实模型、生产迁移、完整线上流程需单独验证；本文不替代测试和生产验收 | 验证边界；“116项测试”不能移用为本仓库证明 |

P0及各G项的具体优先级、依赖、拆分粒度、技术字段和验收用例，是助手根据上述来源及现状提出的执行建议；文件只直接给出“协作质量 → 跨任务调度 → 受控多Agent”的大顺序。会议没有为这些工程拆分逐项指定负责人或工期。明确具名的原事项在来源索引中保留，新增角色仍待认领。

状态含义：**未开始**表示本项所需交付尚无完成证据；**待确认**表示关键产品范围或接入边界尚待具体确认；**已有原型待验证**表示本地代码或 Mock 有基础，真实服务及适用用户闭环仍未通过。文档、示例或组件测试完成，均不能把对应功能勾为完成。

优先级表示建议的实施依赖，不表示已承诺排期：P0 对账与评测准备 → P1 协作质量和最小闭环 → P2 个人分析、任务成本与后续复盘 → P3 受控多 Agent。按U-JOINT，个人与成本的设计现在和EWD一起完成，基础数据及最小人时成本可随EWD试点接入，不等未来再讨论。G14保留待安排状态；G15是局部修正，不据此扩大页面。

## 执行时保持的现行边界

以 [产品决定台账](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/product-v2/09-decision-register.md) 的最新有效决定为准，不把会议方向或本待办自动升级成产品合同：

- D-139 的 Task 唯一行动对象、扁平标签等约束继续有效，不恢复 Todo、Folder、TagGroup、一级团队文件或逐项 Responsibility。完成标准隐藏部分已被后续决定替代，不能继续据此禁止当前标准展示与编辑。
- D-140 的创建暂停已由 D-143／D-147 在本地原型范围内替代。保留“需求 → 必要补问 → 已有任务关系判断 → 候选方案 → 人确认”的任务式页面；不恢复旧聊天气泡、一级新建对话或 D-145 默认空白手工入口。五场景 Mock 不冒充通用真实 AI。
- D-141／D-146 的首页与 AI 连接入口隐藏继续有效；保留轻量顶栏。D-148 保留左侧仅名称的任务索引、右侧详情及列表上下文；Dashboard 暂定，个人分析不得塞进任务索引行或先增加占位页。
- D-142 当前详情仍为讨论／子任务／文件／活动，讨论默认；诊断入口单独确认。D-144 的紧凑头卡、完成标准与小型 Burn up 已有原型，不重新排整页重做，也不借结果确认恢复勾选验收。
- D-149～D-151 的模块范围、差异预览、明确应用、候选过期与失败保留继续有效；非模态浮层收起保留本页输入，手工标准未保存时受保护。候选负责人仍待接受，不静默改 Owner、状态或权限，不增加 AI 审批对象。

各项验收复用 [04 AI 协作质量验收标准](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/04-ai-collaboration-acceptance.md)：注明代码与模型／Skill 版本、环境、输入、实际输出、存储前后状态和失败证据。L1 规则／组件通过不能替代 L2 服务集成或 L3 用户闭环；资料不足记“未知”。原文及验收文档中未获确认的样本量、百分比门槛不作为已定目标。

## P0：把接入范围与评测准备清楚

- [ ] **G01｜对账当前能力与真实 API，形成可联调清单**

  **依据：会议明确待办＋工程范围扩展。**M-API 直接要求整理任务创建真实 API；F-VERIFY 要求真实模型、迁移与线上流程单独验证。

  **为什么跟进：**当前[创建逻辑仍是场景 Mock](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/lib/taskCreationPlanning.ts:42)，[Vite 未挂载旧模型代理](/Users/yxzuji/Documents/ChatGPT/agentdoor2/vite.config.ts:6)。先对账才能知道该接入什么、缺谁提供。把清单扩到身份、文件和会话，是我为核验老板文件的完整协作能力补出的工程范围，**不是会议原待办逐项列出的 API**。

  **优先级：**P0。**当前状态：**未开始；已有差距审计，真实接口、部署和联调状态仍需对账。

  **产出：**一份逐能力对接清单，覆盖身份／团队／成员、任务与讨论读取、权限内查重、规划、确认写入、FileVersion、活动、会话与回传。每项记录真实地址或工具名、提供团队、认证与 ACL、输入输出、版本／幂等、错误、测试环境及最后验证记录；未提供的项目明确列缺口，不虚构 URL。

  **依赖：**无，可与 G02 并行；接口资料或测试权限不足的条目标明待提供方，不填“已联调”。

  **验收：**① 每项区分已有服务、Mock、仅代码和未接入，并能定位证据；② 明确 React 浏览器数据与独立 MCP 存储如何收敛到同一真相；③ 旧模型代理未挂载、连接隐藏、文件上传演示等现状被正确记录；④ 现有头卡、首页精简、页签分离转为接入／验证，不再误列为全新开发。交付清单不代表其中接口全部已上线。

  **建议协调／认领：**卜佳菲协调资料；后端、客户端、安全及各接口提供团队待认领，姓名不构成正式指派。

- [ ] **G02｜Review 现有规划 Skill、Schema 和单／复杂任务样例**

  **依据：会议明确待办与核心方向。**M-SKILL 直接要求单／复杂任务规划逻辑、JSON 示例及准确性验证；F-PLAN 提供目标、拆分、人选和重规划的能力对照。

  **为什么跟进：**仓库已有[规划 Skill](/Users/yxzuji/Documents/ChatGPT/agentdoor2/skills/agentdoor-task-planner/SKILL.md)，所以把原事项接续为“Review 现有产物并解决运行时差异”，避免重复写一套。契约版本、最多两项补问等细则来自现行项目规则，不冒充会议原文。

  **优先级：**P0。**当前状态：**待确认；现有 Skill、契约和示例可复用，本轮产品与运行时取舍尚需 Review。

  **产出：**一版注明版本的规划规则／契约及问题清单，逐项记录意图路由、目标忠实、轻任务不过拆、复杂交付边界、候选人员、必要补问、期限／依赖及查重状态；每个争议保留“现行规则、建议、影响、待确认人”。不从零再写一套平行 Skill。

  **依赖：**无；真实成员／权限字段结合 G01 校准。估算和完成计量只核对与 [06 EWD 详细方案](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/06-ewd-design.md) 的契约衔接，不在本项另立 EWD 算法或实现清单。

  **验收：**① 用真实业务材料及脱敏授权上下文审阅单任务、复杂任务、模糊需求、查询和修改；② 轻任务不产生同名子任务，复杂拆分覆盖独立交付，每项候选人有依据或留空；③ 修改／查询不走创建，问题最多两项且不编造期限、成员或查重成功；④ 记录接受的修订、未决项与适用范围，输出合法 JSON，不把 ready 或模型文字当成写入授权。

  **建议协调／认领：**任向晖协调，卜佳菲参与产品取舍；模型工程及契约适配研发待认领。

- [ ] **G03｜将 AI 验收规范转成冻结案例与版本比较记录**

  **依据：会议方向＋用户分析要求＋工程拆解。**M-SKILL 要求验证模型准确性，U-ANALYSIS 要求 AI 质量和系统差距分析，F-VERIFY 提醒不能用材料代替真实验证。

  **为什么跟进：**当前 Mock 与本地规则测试不能证明大模型能理解真实需求，需要可重复比较的输入、人工判断和失败记录。**冻结案例、重复运行、版本比较是我提出的评测方法，不是会议明确布置的独立待办**；样本量、准确率门槛也不能当作原定标准。现状证据见[05 的验证范围](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/05-current-gap-analysis.md)，其本身不是需求来源。

  **优先级：**P0。**当前状态：**未开始；已有验收文档及本地测试，不等于真实模型评测集已运行。

  **产出：**可复现的案例集、人工标注表和运行记录模板，包含授权范围、输入、期望行为、禁止结果、可接受答案集合、输出及人工修改类型；覆盖解析、摘要／诊断、安全、多语言与后续个人分析的边界案例。对未纳入本轮的能力标出范围，不要求全部未来能力现在通过。

  **依赖：**G02 的候选版本；真实模型运行需 G01 的环境和对应接入授权，离线夹具准备可先进行。

  **验收：**① 每例有版本、来源与期望，不只检查 JSON 形状；② 重复运行、失败、弃答和模型波动均留证；③ 区分事实准确、覆盖、证据引用、权限及副作用，越权／假完成不得被平均分抵消；④ 真实模型结果与 Mock 测试分开，样本不足不报“已达标”或伪精确总分，未批准数值门槛保留待确认。

  **建议协调／认领：**任向晖协调业务标注；测试、模型工程与安全用例负责人待认领。

- [ ] **G14｜备齐下次 Skill 与缺口 Review 的可审阅材料**

  **依据：会议明确后续事项＋执行拆解。**M-REVIEW 要求再次开会 Review Skill；M-SKILL 规定主要审阅内容。

  **为什么跟进：**需要让审阅人看到真实输入、计划差异与失败原因，才能评价解析质量。**会议要求的是召开 Review，本项只是我拆出的材料准备，不覆盖会议安排或实际审阅**；材料齐备不能把原会议事项标为完成。“下周”缺原会议日期，不能据此推算预约时间。

  **优先级：**待安排；材料随 P0 准备，不代表会议已定期。**当前状态：**未开始。

  **产出：**一份 Review 材料包：真实需求及授权上下文、单／复杂任务 JSON、两版计划差异、失败案例、人工修改理由、G01 接口缺口和本清单待确认事项；区分需做决定、需补证据和仅供知悉的内容。

  **依赖：**G01～G03 的阶段性材料；可以先编排已知资料，但缺项明确列出，不为凑齐材料伪造模型结果。

  **验收：**① 审阅人能据同一输入比较目标、拆分、人选和依赖变化；② 每个需要决定的问题有影响与可选处理，不只展示 UI；③ 结论仅在实际审阅后记录，未审阅不写“已确认”；④ 本项只交付材料，不自动发送邀请、创建日历、通知他人或承诺会议日期。

  **建议协调／认领：**任向晖、卜佳菲建议共同协调；研发／测试材料提供者待认领，会议安排另行沟通。

## P1：完成协作质量与单客户端闭环

- [ ] **G04｜接入授权上下文、真实解析和权限内查重**

  **依据：会议功能方向＋老板文件当前能力主张。**M-CREATE 明确自然语言创建／编辑／查询、拆分依赖与权限内查重；M-SKILL、F-PLAN 支持目标、人选和动态规划能力。

  **为什么跟进：**[当前解析](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/lib/taskCreationPlanning.ts:42) 只匹配五个示例，不能据此认为自由需求和跨成员查重已可用。服务端权限过滤、协议适配和确定性校验是实现这些要求的工程建议。呈现沿用 D-147 的任务式页面，这是对会议“对话承载”的后续具体调整，不恢复旧聊天入口。

  **优先级：**P1。**当前状态：**已有原型待验证；五场景 Mock 可试用，真实模型及服务接入未完成。

  **产出：**服务端生成的最小授权上下文、意图路由与规划结果、权限内相似任务检索、新旧协议适配及当前任务式页面的接入；复用现有候选方案和局部调整，不新增聊天入口。

  **依赖：**G01～G03；真实模型／数据接入范围须具体确认。解析阶段只生成候选，正式写入由 G05 承担。

  **验收：**① 受测自由需求得到对应计划或正确澄清，不靠五个原文匹配兜底；② 查询不误建、修改不重建整棵树，人工确认字段与稳定 ID 保留；③ 服务端先 ACL 再检索，不可见任务不进入模型上下文，未查／失败／部分覆盖与无匹配分开；④ 非法成员、悬空／循环依赖、日期冲突由确定性校验拦截；⑤ 相似、父子归属、真实依赖分别解释，未经确认不合并、不创建、不指派，旧响应不覆盖新上下文。

  **建议协调／认领：**卜佳菲协调入口与错误呈现；模型、后端及权限研发待认领。

- [ ] **G05｜统一 React／MCP 的草稿确认、创建和局部编辑命令**

  **依据：老板文件安全目标下的工程推导，非会议直接待办。**F-WRITE 明确权限、当前版本和幂等检查；M-API 为确认真实对接边界提供依据，但没有指定统一 React／MCP 的架构。

  **为什么跟进：**[浏览器创建写 localStorage](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/App.tsx:309)，[MCP 写独立 JSON 文件](/Users/yxzuji/Documents/ChatGPT/agentdoor2/mcp/server.ts:15)，目前无法保证另一客户端看见同一任务及其最新版本。统一命令和数据源是我提出的最小一致性方案；若 G01 找到可复用的既有服务，优先适配，不据此新建第二套平台。草稿 revision、事务及数据接续方式同属工程设计。

  **优先级：**P1。**当前状态：**已有原型待验证；已有本地创建、差异预览与存储恢复，统一服务和跨端写入未完成。

  **产出：**共同任务数据源及确认／创建／编辑命令，绑定 principal、授权范围、草稿 revision、对象版本、幂等键和真实活动；保留当前本地草稿、人工输入及失败恢复语义，定义浏览器旧数据接续方式而非静默清空。

  **依赖：**G01 的数据源与接口边界；可先明确命令契约，真实解析联调接 G04。服务写入范围及数据接续方式具体确认后实施。

  **验收：**① 相同请求重试只生效一次，同键不同载荷明确冲突；② 旧版本并发编辑不静默覆盖，修改后旧确认失效；③ 失败不留下半棵任务树、错依赖或假成功活动，草稿可恢复；④ 其他成员人选未接受不成为正式 Owner／参与关系；⑤ 局部编辑只改指定字段，无变化不记活动，另一授权客户端重读一致；⑥ 按 D-150 区分创建方案直接同步、必要字段最终校验与已有详情未保存标准保护，保留 D-151 浮层预览应用和 EWD 独立确认。

  **建议协调／认领：**后端、MCP、前端及数据接续研发待认领；卜佳菲建议协调确认与失败体验。

- [ ] **G06｜接通标准版本、FileVersion、真实活动和受控结果确认**

  **依据：老板文件能力主张＋未来建议＋会议方向。**F-WRITE 明确文件版本、差异、归因和活动；F-PLAN 提及独立验收，F-QUALITY 建议成功标准检查；M-DETAIL 要求讨论与系统动态分离，M-EWD 要求可信地评估工作量完成。

  **为什么跟进：**[文件列表仍使用组件内状态](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/components/task-files/TaskFileExplorer.tsx:10)，[上传按钮标为演示](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/components/task-files/TaskFileExplorer.tsx:49)，不足以支持可追溯诊断或完成计量。版本化标准、精确证据绑定和确认失效规则是我补出的落地方案，**原文没有规定这套数据结构，更没有要求新增审批或勾选验收流程**。

  **优先级：**P1。**当前状态：**已有原型待验证；标准文本、文件预览和活动分区已有，真实证据链未完成。

  **产出：**可追溯的完成标准版本、文件上传／新版本／读取能力、精确证据引用、真实前后差异与 Human／Agent 归因；明确有权人对结果的确认依据、权限、版本和失效条件。复用 Task、File／FileVersion、Activity 等对象，不新增 AI 审批状态机或逐项 Responsibility。

  **依赖：**G01／G02，可与 G04 并行；跨端一致性与 G05 联调。与 [06 EWD 详细方案](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/06-ewd-design.md) 的边界是：本项提供可信标准、成果与结果确认事实，G07 定义其如何进入工时计量和进度事件；本项不重复实现 EWD 聚合或 Burn up。

  **验收：**① 从标准能定位准确文件及版本，旧引用可追溯，权限撤销后不可继续读取；② 上传或写版本失败不显示完整成功，刷新及另一授权客户端读取一致；③ 变更记录的操作者、时间、前后值与存储一致，不按当前值补造历史；④ AI 检查、执行者声明和有权人的结果确认严格区分；⑤ 标准或文件变化后旧检查／确认是否适用明确标识，不自动验收、不恢复标准勾选 UI，讨论仍不混入系统流水。

  **建议协调／认领：**卜佳菲协调完成与证据口径；后端、文件存储、权限及客户端研发待认领。

- [ ] **G08｜打通一个客户端的授权会话、执行报告和安全成果回写**

  **依据：会议明确能力方向＋老板文件当前能力主张。**M-REPORT 要求回传规范、连接校验与心跳；F-SESSION、F-WRITE 描述个人 Agent 会话、所有人授权和成果安全写回。

  **为什么跟进：**[当前连接组件](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/components/AiConnectionDialog.tsx:123) 复制上下文或唤起客户端后用定时器置为连接，不能证明真实握手、执行报告及成果回写。先做一个客户端是我提出的最小范围；事件序号、重试和撤权校验是工程拆分。会议接入方向不等于现在获准恢复被隐藏的入口。

  **优先级：**P1。**当前状态：**待确认；当前未形成完整 Work Session／Agent 授权／回传链路，接入入口仍隐藏。

  **产出：**一个明确客户端的最小会话协议及接力样例，包含授权范围、有效期、撤销、运行／事件 ID、序号、上下文版本、执行声明、交付物引用与回执；技术握手和心跳独立记录，不混入业务进度和人的讨论。

  **依赖：**G01／G05／G06。协议与受控测试准备不等于开放入口；恢复连接入口、调用真实客户端和执行授权须分别明确适用范围，不绕过 D-141／D-146。

  **验收：**① 在指定测试范围走通请求 → 所有人授权 → 执行 → 写回 → 重读，未处理／拒绝不启动；② 每次读写复核权限，会话过期或撤权后立即拒绝；③ 重复、乱序、时钟偏差、断线重连、并发及写入失败不产生重复效果或假完成；④ 成果保留版本与授权人／Agent 来源；⑤ 心跳只证明连接，不将离线、沉默或工具退出成功解释成人没工作或业务已验收。

  **建议协调／认领：**后端、客户端、MCP 与安全研发待认领；具体客户端及技术协调人待确认。

- [ ] **G09｜实现按需风险诊断、完成标准检查和协作摘要**

  **依据：会议明确待办＋老板文件未来推荐。**M-DETAIL 直接要求洞察升级为独立 Tab 或同级模块；风险识别、标准检查、摘要的组合来自 F-QUALITY，不能都称为会议原定功能。

  **为什么跟进：**当前[详情页签](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/components/TaskDetail.tsx:289) 没有诊断模块，历史建议也不能提供基于当前证据的执行分析。把上述能力组合成任务诊断，可回答“卡在哪里、缺什么、下一步做什么”。**原文件建议每日协作摘要，本方案先收窄为按需生成**；没有覆盖定时生成或按收件人聚合通知，具体入口仍待确认。

  **优先级：**P1。**当前状态：**待确认；当前没有开放的真实诊断闭环，旧洞察组件和历史 AI 建议不能算已完成。

  **产出：**按需生成的任务分析，覆盖真实阻塞、期限或目标风险、标准证据缺口及讨论／活动摘要；每条包含结论、证据版本、覆盖／缺失、生成时间和可执行下一步。同时给出独立诊断承载的最小方案，入口确认后再挂载。

  **依赖：**G04／G06；Agent 执行证据再接 G08。涉及工时／趋势的结论仅在 [06 EWD 详细方案](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/06-ewd-design.md) 对应数据可用后启用，基础诊断不必等待 G07 全部完成。新诊断 Tab 仍需明确调整 D-142 范围。

  **验收：**① 每条结论能回到当时可见的证据，无证据、过期、矛盾及无权限分别表达，不硬造风险；② 标准检查区分已有依据／不满足／未知，不自动验收或改变状态；③ 摘要保留已决、未决、阻塞与下一步，负责人提议不当作已接受；④ 旧响应不能覆盖新版本，失败保留上次结果并说明过期；⑤ 不冒充人的发言，不恢复旧巨型概览或把洞察侧栏直接放大。

  **建议协调／认领：**卜佳菲协调任务诊断内容与入口；模型、后端和前端研发待认领。

- [ ] **G15｜让详情依赖提示只在实际阻塞时占用头部**

  **依据：会议减法方向＋项目设计规则的具体转译。**M-DETAIL 要求降低前置依赖的显性呈现；项目设计规则 D07 将其细化为只在实际阻塞时提示“等待 X”。老板文件的跨任务依赖检测是另一项能力。

  **为什么跟进：**[现有显示条件](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/components/TaskDetail.tsx:349) 只判断有无依赖，已满足前置也会占用头部。按阻塞状态控制是降低认知负担的局部建议，**不是会议逐字指定的判断条件**；面包屑表达归属，不能拿它代替真实依赖信息。

  **优先级：**局部修正；可在 P1 主线旁推进，排期未定。**当前状态：**已有原型待验证；现有条件只判断有无依赖，尚未按实际阻塞控制。

  **产出：**基于明确依赖满足口径的紧凑“等待 X”提示及按需查看路径；沿用归属面包屑，不新增关系大图、概览或一级入口。

  **依赖：**核对 [基本设计原则](/Users/yxzuji/Documents/ChatGPT/agentdoor2/skills/agentdoor-task-design/SKILL.md) 与实际前置满足语义；若判断依赖结果确认，消费 G06 的有效事实，不擅自把任务状态等同验收。

  **验收：**① 已满足的前置不持续占据详情头部；② 真正阻塞显示可追溯的等待对象、原因及可执行查看动作；③ 缺记录、失效引用或无权读取不误报“可开始”，明确提示待核对；④ 父子归属不当作依赖；⑤ 从提示打开相关任务及返回时保留列表搜索、筛选和位置，其他依赖按需仍能查阅。

  **建议协调／认领：**卜佳菲建议核对提示与满足口径；前端研发待认领，必要的依赖数据由后端协作。

## P2：个人优先级与任务成本——与 EWD 联合设计

本节的两个产品能力是 **G11个人优先级／紧急程度** 与 **G16单任务成本**，共用G10和EW-08的数据；G12保留为有真实历史之后的计划复盘与推荐。设计见06第0/11/15/16节，P2不表示延后本轮分析，也不表示所有成本数据必须等待AI诊断完成。

- [ ] **G10｜补齐本人工作窗口、分人剩余投入和目标／期限依据**

  **依据：会议个人方向＋本轮联合要求下的数据前置。**M-PERSON、U-ANALYSIS要求本人应推进什么以及重要／紧急、工时分析；U-JOINT要求与任务成本、EWD一起设计，F-SCHEDULE提供未来容量建议。字段与窗口规则由06按U-PM提出，不是原文已有清单。

  **为什么跟进：**现有[成员来源](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/App.tsx:165) 为场景数据，[推荐规则](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/lib/responsibilityAssignment.ts:24) 依赖职责关键词和可用性文字，不是可信的个人容量记录。没有本人剩余投入和可用窗口，就不能回答截止前能否完成。工作日历、分人投入及父子去重都是我提出的数据设计，不能声称会议已经定了人时算法。

  **优先级：**P2；共享数据可随EWD基础并行。**当前状态：**联合设计已补充，数据接入未开始。

  **产出：**已生效负责关系、已接受执行投入、目标影响证据、硬/软截止及其时区、工作窗口、固定占用与前置可用时间；附来源、权限、更新时间和缺失状态。分人剩余来自06/EW-08，与G16共用；本项不另建EWD估算或成本账本。排序仅本人负责范围，但容量须计入本人在其他任务的有效承诺，不能只看Owner列表。

  **依赖：**G05／G06；共享剩余投入字段与 06 对齐。个人信息来源与可见范围在采集前明确，不将工具在线数据变相用作工时记录。

  **验收：**① 本人有效承诺不被筛选/分页截断，Owner与已接受执行安排分开；② 父子和多人不重复、他人投入不计给本人；③ 截止/窗口/前置有来源，Task预约不在日历扣减和需求中双算；④ 缺数据不当零，个人可读不扩大到隐藏Task内容；⑤ 通过06的JA-04～07/09/12数据边界，保留人工修订与撤权处理。

  **建议协调／认领：**产品及后端研发待认领；卜佳菲建议协调个人输入体验，实际成员自行确认自己的可用窗口。

- [ ] **G11｜试用可解释的个人行动列表与重要／紧急判断**

  **依据：会议分析范围＋本轮明确要求＋老板未来机会。**M-PERSON、U-ANALYSIS、U-JOINT直接要求本人优先级/紧急程度；F-SCHEDULE提供Workspace优先级方向，范围依会议收窄为本人。U-PM要求方法支撑，06据此区分目标价值、资源约束与行动选择，不声称PMP有统一打分公式。

  **为什么跟进：**当前任务索引和本地任务字段尚不能给出有证据的个人行动建议，见[现行任务数据](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/data/workspaceNodes.ts:15)。需要回答“为什么现在先做这件事”，而非只显示任务数量。轻量行动列表、不设伪精确总分和保留人工调序是我的产品方案；**会议未规定 Dashboard 布局，老板文件也没有要求新建个人 Dashboard**。

  **优先级：**P2。**当前状态：**联合规则已形成提案，具体分档与入口待确认，未实施；Dashboard不在当前开放范围。

  **产出：**仅针对本人有效负责范围的轻量分析列表，每条回答“推进哪个任务、具体做什么、为何现在做、依据和更新时间”；区分可执行、等待前置、需要本人决定及信息不足，支持人工纠正并回到既有任务处理。

  **依赖：**G10与EW-08的共享数据；确定性冲突和手工规则可先验证，AI诊断证据再复用G09，不要求先完成全部AI能力。算法、覆盖和入口须确认；不恢复首页/注意力地图/团队排行，不往D-148名称索引堆分析字段。

  **验收：**① 用户能说明下一动作与理由；② 重要性/紧急性/工时/成本分开，已逾期事实不因缺估被抹掉；③ 检查组合容量、前置最早可用与连续工作块，D≤H不能冒充排程可行；④ 未知不自动沉底，保留人工顺序但不隐藏客观冲突；⑤ 通过JA-04～07/10/12，不以已花成本提高优先级、不自动改任务或通信，不扩大为绩效评判。

  **建议协调／认领：**卜佳菲协调小范围使用验证；模型、前端及数据研发待认领，试用对象与范围待确认。

- [ ] **G16｜实现单任务投入成本分析，与 EWD 和个人分析共用事实**

  **依据：用户本轮明确要求，不是原会议既定模块。**U-JOINT直接要求“任务下成本分析”与EWD一起分析；U-PM要求方法支撑。M-EWD提供人时基础，F-SCHEDULE的偏差复盘可复用结果，但老板文件没有指定完整成本功能。人时优先、金额可选和历史归属规则是06的设计提案。

  **为什么跟进：**[当前Task类型](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/data/workspaceNodes.ts:15)没有实际/剩余投入与费用记录，EWD曲线不能回答已经花了多少或还需多少。直接用当前叶子汇总成本会在取消/重挂时丢失已投入，因此需共享工时事实并单独保留历史归属；不是再建一套EWD或完整财务系统。

  **优先级：**P2联合能力；最小人时读数可随EWD试点并行。**当前状态：**详细设计已形成，口径/入口待确认，未实施。

  **产出：**任务下按需展示当前预计E、已记录A、独立剩余R、可比且完整时F=A+R，解释范围/返工/改估差异及可选管理动作；工作量P仍独立。金额仅在范围、币种、费用与权限确认后另切片，人工参考折算不冒充实际支付。原基准、当前确认、最新预测分别保留，不预设CPI/SPI。

  **依赖：**G05的正式存储与版本、EW-02的E、EW-08的A/R及历史分配；G06的验收事实用于P，不是承认实际投入已经发生的前置。与G10/G11共用R，不复制个人排序。G12后续读取本项历史复盘；金额不得成为首版人时分析的门槛。

  **验收：**① JA-01～03成立，花完工时不等于完成，取消/重开不抹历史；② 当前范围与生命周期口径分开，父子/多人/重挂按记录去重；③ 缺A/R不造F，改估不覆盖基准；④ 金额切片通过JA-08/12，固定费/用量/外包不双算，费率撤权不泄露；⑤ 不把当前EWD当EVM基准、不把CPI/SPI换成延期天数；⑥ 结论有证据和下一步，入口不违反D-142/D-148。

  **建议协调／认领：**产品、后端及前端待认领；金额范围启用时再明确费用数据提供方与权限，不自动指派成员。

- [ ] **G12｜用真实历史做计划偏差复盘与容量感知推荐**

  **依据：老板文件未来机会，非会议明确待办。**F-SCHEDULE 直接提出“容量感知的成员推荐”“计划偏差复盘”；与用户工时分析问题相关，但不据此改写为会议已要求本轮实现。

  **为什么跟进：**只有积累可信历史，才能解释计划偏差并改善后续推荐；[当前 Burn up 只投影传入快照](/Users/yxzuji/Documents/ChatGPT/agentdoor2/src/lib/taskBurnUp.ts:88)，不提供这类历史依据，因此列为后续候选。原文件说的是“预计与实际工期、返工和阻塞原因”；进一步区分范围、等待、实际人时与剩余人时，是本轮方案的扩展，不能把工期直接当人类工时。

  **优先级：**P2。**当前状态：**未开始；需先积累可信历史，现有示例曲线不能作为复盘依据。

  **产出：**可回看证据的偏差解释与候选调整，区分范围改变、等待、返工、投入分配和估算偏差，并用于之后的排期／人选建议。复用06的基线、EW-08投入及G16成本历史，不另造事件/账本；**G16分析当前任务，G12把可比历史用于下一次计划**，两者不重复。

  **依赖：**G07 在 06 中约定的相关历史能力、G10 的个人窗口与分人投入；仅在记录覆盖足以支撑具体结论后验证，无历史时保留为未开始。

  **验收：**① 原计划、范围变更、等待及投入记录各自可追溯，不用今天的估算改写当时承诺；② 实际记录缺失不当零，不把未验收工作量当剩余耗时；③ 容量建议按同一时间窗口、完整承诺与真实可用时间解释，缺数据说明未知；④ 推荐人选先满足授权与职责证据，选择仍是候选，不自动移交；⑤ 复盘输出可调整的计划因素，不推断个人绩效或宣传无可比基线的 AI 提效。

  **建议协调／认领：**产品、数据与模型研发待认领；业务复盘协调人待确认。

## P3：最后讨论受控多 Agent

- [ ] **G13｜形成受控多 Agent 与云端持续运行的立项方案**

  **依据：会议长期方向＋老板文件未来方向。**M-CLOUD 指向企业云端 Agent 持续运行与治理；F-MULTI 提出专业分工、授权后的定时／事件执行和组织经验复用。

  **为什么跟进：**这些方向需要在 G08 单客户端闭环成立后判断价值与边界，现有[MCP 工具](/Users/yxzuji/Documents/ChatGPT/agentdoor2/mcp/server.ts:41) 不等于云端持续运行层。**本项只准备立项方案，是对原文未来实现方向的阶段收窄**；预算、隔离、停止控制和阶段放行是我补出的安全设计，不表示会议要求现在建设完整治理平台。

  **优先级：**P3。**当前状态：**待确认；属于另行立项，不因本清单启动云端运行。

  **产出：**一个边界明确的演进方案，说明何时需要多 Agent、适用任务、唯一 Human Owner、授权／停止／撤销、预算、重试、隔离、恢复与经验复用；选定一个受控验证场景和分阶段放行条件，不先搭治理驾驶舱。

  **依赖：**G08 的单客户端闭环及安全场景通过；在协作质量、个人／跨任务能力的实际使用证据基础上说明扩展必要性。技术负责人、范围、资源及执行授权另行确认。

  **验收：**① 方案可说明单 Agent 不足及最小新增能力；② 未授权、拒绝、过期或撤销后不得继续运行，停止和预算限制有确定控制点；③ 并发、失败、重试及接续不重复写入、不扩大权限，结果与授权来源可追溯；④ 每项正式 Task 始终保留唯一 Human Owner，Agent 不成为责任替代；⑤ 方案 Review 完成只代表立项材料可用，不能据此声称多 Agent 功能已完成或开始定时执行。

  **建议协调／认领：**任向晖建议协调演进方向；技术负责人以及云端、客户端、安全研发均待确认或认领。

## 来源覆盖与本轮未纳入的内容

本清单不是把两份来源的每句话都变成开发任务。已在当前原型完成的头卡、首页精简和页签分离，归 G01 核验；EWD／Burn up 归 06 的 EW-01～08；G14 只准备材料，原会议的安排与实际 Review 仍待跟进。以下来源只被局部覆盖或暂未纳入，**不自动新增待办编号或承诺实现**：

| 原始内容与来源 | 本轮边界及原因 |
| --- | --- |
| 智能通知摘要、每日协作摘要（F-QUALITY，未来推荐） | G09 只做按需任务摘要，不包含通知去重、按收件人聚合或定时推送；避免恢复会议要求移除的通知聚合工作台，后续需另确认必要性 |
| 跨任务依赖检测（F-SCHEDULE，未来机会） | G04 判断创建上下文关系，G15 呈现已有依赖；没有承诺持续扫描所有任务、主动发现依赖／重复／期限冲突 |
| 更多客户端完整同步（F-MULTI，未来方向） | G08 只验证一个客户端；不能声称已覆盖所有客户端，扩展需先取得单客户端闭环证据 |
| 专业 Agent 分工、定时／事件运行、组织经验复用（F-MULTI，未来方向） | G13 仅研究立项方案，未纳入功能实施，也未授权后台持续运行 |
| 可复用 AI 标签、标题／目标变化后自动更新（F-INTEL，当前能力主张） | 当前待办未完整承接；先在 G01 验证实际能力，再决定范围，不因原文件写“已有”就当作本仓库已实现 |
| 多语言规划与文字系统校验（F-INTEL，当前能力主张） | G03 仅纳入评测准备；支持语言与具体实现范围留给 G02／G04 核对，不代表已经通过 |
| 读取讨论后建议下一批子任务（F-PLAN，当前能力主张） | G04 包含部分动态规划基础，未单独承诺完整下一批任务流程；局部编辑不能证明这项已完成 |

## 如何接续与勾选

先从G01/G02准备，G03固定案例，G14汇总审阅材料。真实解析与协作闭环继续按G04～09衔接；G15可独立核对。按最新要求，EWD、个人优先级和任务成本已在06联合设计；先由G10/EW-08准备共享事实，G11/G16验证个人与任务两个视角，G12待积累历史再做推荐复盘。手工数据分析不必等通用AI接入；G13仍在单客户端闭环与扩展必要性获证后另行立项。

待办被承接时再补实际认领人、明确范围和验证环境；没有承接记录就保留“待认领”。对应产出与全部适用验收完成后，附证据位置、实际版本和未决限制，再考虑勾选。仅完成设计文档、提供一个 Mock、通过若干组件测试或展示截图，不足以勾选真实功能项；G01／G02／G03／G14／G13 这类资料与方案项即使日后勾选，也不能代替下游功能验收。
