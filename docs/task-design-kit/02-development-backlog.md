# 开发清单与真实 API 核对表

> 本页保留初版开发提案，不再表示“下列均未开始实施”。当前源码差异、已完成原型与剩余工程见 [05 当前差距与补齐待办](/Users/yxzuji/Documents/ChatGPT/agentdoor2/docs/task-design-kit/05-current-gap-analysis.md)：创建按 D-147 的任务式 Mock 流程，头部、导航和讨论/活动分离已有实现，Burn up 已有 SVG 展示，真实解析与数据闭环仍待接入。会议具名负责人只代表原待办归属，不表示接受了新增工程工作；研发/安全负责人待认领，不编造日期或工期承诺。

## 1. 初版先后顺序与验收（不作为当前状态表）

| ID / 优先级 | 可执行工作 | 负责人 | 依赖 | 完成证据 |
| --- | --- | --- | --- | --- |
| K01 / P0 | Review 本包六个待决策点，记录与 D-139/D-140 的关系 | 任向晖、卜佳菲；最终批准人确认 | 无 | 决策台账明确采用/保留/推迟；未批准不恢复入口 |
| K02 / P0 | 核对真实任务、成员、权限、创建及回传 API | 卜佳菲协调；后端待认领 | 无，可并行研究 | 每项有真实接口文档、owner、鉴权、错误码、测试环境及联调证据 |
| K03 / P0 | Review 单/复杂任务 Skill 与 JSON 契约 | 任向晖 | 本包 | 目的/规模/拆分/推荐/期限可复核；异常输出有明确状态 |
| K04 / P0 | 建立跨行业离线评测集及版本比较 | 任向晖协调；工程待认领 | K03 | 校对、活动、采购、软件、研究等场景；越权和未确认写入为零容忍，语义质量由人盲评 |
| K05 / P1 | 服务端 ACL 上下文装配 + 权限内查重 | 后端/安全待认领 | K02、K03 | 双团队隔离；权限撤销复核；检索超时/截断显式标注；模型看不到无权对象 |
| K06 / P1 | 新旧协议适配、JSON 校验和计划校验 | 工程待认领 | K03、K04 | ID 稳定；循环/悬空依赖、无效成员、不可能日期被拒；用户已确认字段不丢失 |
| K07 / P1 | 原子创建命令与确认回执 | 后端待认领 | K01、K02、K05、K06 | draft revision+confirm token+幂等；重试不重复建；失败无半棵任务树；Owner 未接受不伪装成立 |
| K08 / P1 | 恢复任务上下文对话入口与可编辑草稿 | 卜佳菲设计；前端待认领 | K01、K07 | 无新增一级“新建对话”；确认前可改；取消不写入；编辑/查询正确路由或明确暂不支持 |
| K09 / P1 | 概览变为紧凑 Heading Card | 卜佳菲设计；前端待认领 | K01 | 无重复概览和错误层级；长标题/空期限/窄屏/键盘通过 |
| K10 / P1 | 首页纯任务列表、导航精简 | 卜佳菲设计；前端待认领 | K01 | 注意力地图/通知聚合退出默认工作台；保留必要查找与通知入口 |
| K11 / P1 | Discussion / Files / Activity 分离 | 卜佳菲设计；工程待认领 | K01、消息与日志契约 | 自动事件不进入讨论流；迁移不丢记录；深链接仍可用 |
| K12 / P1 | EWD 字段、估算确认、历史事件与聚合 | 卜佳菲定义；后端待认领 | K01、K02 | 2/20=10%；父子不重复；未知不等于0；新增、取消、重开、改估可回放 |
| K13 / P1 | Burn up 三候选 PoC、确定库与交互 | 卜佳菲调研；前端待认领 | K12口径；可先用明确Mock | 两条阶梯线+事件说明+数据表；检查许可证、依赖、性能与无障碍；不拿演示曲线当真实历史 |
| K14 / P1 | 任务诊断 Tab 与证据链接 | 卜佳菲设计；工程待认领 | K09、K12；趋势依赖K13 | 不留右侧重复AI栏；每结论有依据/新鲜度/覆盖；数据不足不编造 |
| K15 / P1 | MCP 连接真实校验和回传契约 | 后端/客户端待认领 | K02 | 握手/断线/撤销/时钟偏差/重放/乱序可测试；不从心跳推断人是否工作 |
| K16 / P2 | 云 Agent 最小运行与治理设计 | 任向晖协调；技术owner待认领 | K07、K15验证后 | 明确授权、终止、预算、重试、结果验收边界；另立项，不默认实施 |
| K17 / 待安排 | 下次集中 Review Skill | 任向晖、卜佳菲 | K03、K04 | 时间由二人确认；展示真实输入、输出、偏差和版本差异；本轮未预约会议 |

推荐交付切片：先 K01–K04；然后 K05–K08 的创建闭环；K09–K11 可在设计批准后独立推进；K12–K15 完成诊断闭环。不是先画完所有页面再补权限和数据。

## 2. 代码证据与 API 清单

核对方式：2026-08-31 静态查看当前工作区，未调用真实业务服务。下面只有标注“代码存在”的名称是真实发现；其余是能力契约，**不虚构 URL**。

| 能力 | 已发现证据／状态 | 上线前还需对接或验证 |
| --- | --- | --- |
| 模型任务解析 | `server/taskAssistant.ts` 保留 POST `/api/task-assistant` 插件定义与 Responses 风格上游调用；当前 `vite.config.ts` 未挂载插件，前端未调用 | 真实运行时接入、生产部署与鉴权、租户上下文、限流、可观测性；当前不能视为可用 API |
| 草稿请求响应校验 | `src/lib/taskAssistantProtocol.ts`：Zod、mainTask/subtasks、索引依赖、人员推荐 | 新契约版本适配；达成标准、Tips、EWD、查重覆盖、稳定ID及语义校验 |
| 当前任务/成员上下文 | 现有 request 接收 existingTasks（最多80）和 members（最多60） | 服务端根据会话与 ACL 装配，不能信任浏览器传来的 currentUserId/成员列表作为授权依据；截断必须返回coverage |
| 模型上下文上游 | `server/taskAssistant.ts` 中上游 URL 与调用实现 | 密钥不进客户端；错误响应脱敏；不能据此认定业务 API 已齐全 |
| 原型节点创建 | `src/lib/workspaceTaskCreation.ts` 返回内存 nodes；`src/App.tsx` 存在 localStorage 持久化代码 | 不是组织级事务API；统一稳定ID、权限、并发、幂等与跨端持久化 |
| 独立 MCP 草稿/创建 | `mcp/server.ts` 有 `agentdoor_prepare_task` / `agentdoor_create_task`，内存草稿和文件存储 | 与React数据源尚不能视为统一；真实成员ID、鉴权、确认凭据、原子性、重启/并发幂等、日期默认需审查 |
| 用户身份/组织团队权限 | 本轮未确认生产 API | principal、teamId、可见/可创建/可指派权限及撤销行为 |
| 成员目录与职责 | 本地数据与协议字段存在；未确认生产 API | ID、职责证据、本人公开且会过期的协作窗口；不使用在线活动推算容量 |
| 相似任务检索 | request有existingTasks，不等于专门查重服务 | ACL过滤后检索、taskId/标题/证据、coverage、超时/部分失败 |
| 正式批量创建/编辑/查询 | 未确认统一生产 API | createPlan、readTask、searchTasks、proposePatch、commitPatch 的实际接口；批量原子事务与revision冲突 |
| 讨论/文件/动态 | UI和Mock存在 | 分离类型、证据权限、分页、文件版本引用、删除与审计策略 |
| EWD/Burn up | 本轮未确认生产数据接口 | 版本化估算、scope事件、验收事件、按时区聚合、数据缺口 |
| 连接状态/心跳/执行回传 | App 存在连接标记持久化；不能证明真实MCP健康检查 | handshake、heartbeat、executionEvent、connectionStatus 的真实接口、鉴权与重放防护 |

每个接口登记模板：能力名｜实际 URL/工具名｜负责团队｜输入/输出 Schema｜鉴权主体与 ACL｜幂等/版本规则｜错误码｜测试环境｜最后验证时间｜证据链接。

## 3. 放行门槛与评测建议

- 所有写入经服务端授权和具体版本确认；模型 ready 标记不是授权。
- 结构校验与语义评测分开：JSON 合法不代表目的正确、拆分合理。
- 建议初始评测 20 个以上固定场景、每场景重复 3 次，冻结模型/Prompt/Schema/输入版本；这是建议规模，不是已完成结果。
- 零容忍：越权内容输出、编造成员ID、静默正式创建、伪造查重成功、伪造承诺。
- 语义项人工评分：目的忠实、不过度拆分、独立交付边界、负责人依据、期限现实性、Tips有效、语言简洁。通过阈值在首轮样本后确认，不预设一个无依据总分。
- 应用实现后按风险运行现有 `npm test`、`npm run build`，以及权限/批量事务联调和 UI 状态验收；本次文档交付不声称这些应用验证已经通过。
