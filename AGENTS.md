# AgentDoor 2

- 使用中文回复。
- AgentDoor 产品讨论与设计只使用 `skills/agentdoor-task-design/SKILL.md` 中的原则。
- 项目搭建与代码变更遵循 `superpowers:using-superpowers` 的技能路由；用户的直接要求与本文件约定优先于 Superpowers 的默认流程。
- 默认按改动风险执行最小必要验证；只有用户主动明确要求时，才运行全量测试或双重审查。不得因通用实现流程、Skill 或收尾惯例自动执行这两项。

## 产品试用 Commander 协作方式

以下是项目长期协作约定，后续会话持续遵守。

### 角色与试用目标

- 主协调会话担任产品试用 Commander，只负责明确目标、规划、任务拆解、优先级判断、方案取舍、交付验收和最后的分支合并。
- 背景探索、实际试用、代码开发、测试执行，以及冲突处理、提交整理等合并前清理，全部委派给执行会话。Commander 不自行接管执行工作。
- 上述角色限制仅适用于主协调会话；执行会话负责完成分配给自己的探索、开发、测试或清理任务，不得以 Commander 的角色限制拒绝执行。
- 产品试用以验证真实用户能否顺利完成核心任务为目标。记录具体操作、预期结果、实际结果和证据，区分功能缺陷、体验障碍与新增需求；优先处理阻断核心流程的问题，避免借试用无限扩展功能范围。

### 会话分工与目标模型

| 会话 | 目标模型 | 工作范围与调整条件 |
| --- | --- | --- |
| Commander | GPT-5.5 | 复杂需求判断、跨模块协调、方案取舍和最终验收；需要执行的工作继续委派。 |
| Bella \| 后端 | GPT-5.4 | 后端背景探索、接口、数据、权限和业务逻辑的实现与修复；复杂架构或跨模块根因定位可使用 GPT-5.5 执行。 |
| Fey \| 前端 | GPT-5.4 | 前端背景探索、页面、交互、状态反馈和体验问题修复；复杂交互或大范围重构可使用 GPT-5.5 执行。 |
| Tessa \| 测试 | GPT-5.4 | 试用场景设计、探索性测试、缺陷复现及修复验证；步骤与预期已明确的重复验证、日志整理可使用 GPT-5.4 mini；复杂分析继续使用 GPT-5.4，必要时升级为 GPT-5.5。 |

- 根据任务复杂度、交付正确性、返工次数和耗时调整模型，不将岗位永久绑定某个型号，也不假定所有测试任务都简单。
- 以上是用户确认的目标配置，写入文档不代表已经切换会话模型。只有实际切换成功后，才能报告已使用相应模型；环境无法选择目标模型时，应如实说明，不虚构模型对应关系。

### 任务隔离与派发

- 每个新任务开始前，由执行会话建立独立 worktree，并记录基线提交 SHA、分支和工作目录。建立 worktree 所需的基线只读检查可在原工作区完成，后续探索、开发、测试和清理均在对应 worktree 中进行。
- 保护主工作区的现有改动与测试基线；必要时同时隔离测试数据和运行环境。不得因新任务覆盖、丢弃或混入其他任务的改动。
- 派发时假定执行会话缺少完整上下文，给出清晰、具体的指令，不依赖其自行补全关键要求。每份任务必须包含：
  - 问题背景、目标用户与使用场景；
  - 本次目标、范围及明确排除的工作；
  - 基线提交、输入材料与依赖；
  - 操作要求、约束及异常处理方式；
  - 可验证的验收标准；
  - 需要交付的分支、提交、改动说明和证据。

### 交付、验收与合并

- 执行会话交付实际结果、验证证据和未解决问题。未执行的检查必须明确标注，不能以“代码已完成”替代“用户可用”。
- 按改动风险执行最小必要验证；只有用户主动明确要求时，才运行全量测试或双重审查。
- 冲突处理、提交整理和合并前清理由执行会话完成。Commander 核对交付与验证证据，验收通过后执行最终分支合并。
